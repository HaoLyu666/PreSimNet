# Sample Data

`test_data_sample.npy` contains 128 windows copied from the full test set. It is included only as a schema example and smoke-test input.

The full test set used in the reported metrics has shape `(2246939, 40, 13)` and is not included in this repository.
